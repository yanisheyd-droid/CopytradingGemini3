import { config, validateConfig, runtimeConfig } from './config/environment';
import { listener } from './core/listener';
import { copyEngine } from './core/copyEngine'; // Vérifiez la présence de guillemets droits
import { discoveryWallet } from './core/discoveryWallet';
import { telegramBot } from './telegram/bot';
import { ledger } from './core/ledger'; // Vérifiez la présence de guillemets droits
// ... (reste du fichier)
